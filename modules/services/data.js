// const collection = mongoManager.getCollection('test');
// const data = await collection.updateOne({ ok: 1 }, { $set: { test: true, random: Math.random() } }, { upsert: true });
// logger.debug('data');
// logger.debug(data);